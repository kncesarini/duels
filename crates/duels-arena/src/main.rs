//! `duels-arena`: tournament/Elo runner for pitting `Agent` implementations
//! against each other over many simulated games.
//!
//! M0 placeholder only. A later milestone will add: match scheduling between
//! registered agents, parallel game execution against `duels-core`'s engine,
//! Elo/leaderboard tracking, and result logging for RL training data
//! collection.

fn main() {
    println!("arena: not yet implemented");
}
