[package]
name = "duels-agent-alphabeta"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Expectimax search with alpha-beta pruning, iterative deepening and a transposition table."

[dependencies]
duels-core = { path = "../../duels-core" }
duels-agents-api = { path = "../../duels-agents-api" }
duels-strategy = { path = "../../duels-strategy" }
rand = { workspace = true }

[dev-dependencies]
duels-agent-random = { path = "../random" }
